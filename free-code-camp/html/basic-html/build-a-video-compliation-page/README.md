### Build a Video Compilation Page

This challenge focuses on building a **Video Compilation Page** that organizes multiple videos under different topics using semantic HTML structure. The goal is to practice structuring content clearly and embedding multimedia responsibly.

#### Objective

Create a webpage that contains a main element as the page’s primary container and includes multiple sections, each with a title, description, and embedded video.

#### Key Concepts

1. **Semantic Structure**

   * The `<main>` element should serve as the main container for all visible content on the page.
   * Each `<section>` represents a distinct category or video topic within the page.
   * Using `<h1>` for the main title and `<h2>` for subsection titles provides clear hierarchy and improves accessibility.

2. **Embedding Videos**

   * The `<iframe>` element allows embedding videos from external platforms like YouTube or Vimeo.
   * Proper attributes should be included:

     * **src**: Defines the URL of the embedded video.
     * **title**: Describes the content of the embedded video for accessibility tools.
     * **width** and **height**: Specify the display dimensions of the video frame.

3. **Accessibility and Best Practices**

   * Every embedded video should have a descriptive `title` attribute for screen readers.
   * Grouping related content (heading, text, and video) within `<section>` elements makes the layout logical and user-friendly.

#### Page Layout Requirements

* The **main** element is the only child of the **body**.
* The **h1** introduces the overall topic.
* A **paragraph** directly below the h1 provides context or a short introduction.
* Three **sections** follow, each containing:

  * An **h2** for the video title.
  * A **paragraph** with a short description.
  * An **iframe** embedding a video with defined size and descriptive title.

#### Goal

By completing this challenge, you’ll learn how to organize multimedia content semantically using HTML and enhance accessibility with proper structure and descriptive attributes. This is a foundational skill for creating informative and engaging media-rich web pages.

---

<div dir="rtl">

# ساخت صفحه گردآوری ویدیو

این چالش بر ساخت یک صفحهٔ گردآوری ویدیو تمرکز دارد که چندین ویدیو را زیر موضوعات مختلف با استفاده از ساختار معنایی اچ‌تی‌ام‌ال سازمان‌دهی می‌کند. هدف، تمرین سازمان‌دهی شفاف محتوا و جاسازی درست محتوای چندرسانه‌ای است.

---

## هدف

ایجاد یک صفحهٔ وب که شامل یک عنصر اصلی (`main`) به‌عنوان ظرف اصلی صفحه باشد و چندین بخش (`section`) داشته باشد. هر بخش باید دارای عنوان، توضیح و یک ویدیوی جاسازی‌شده باشد.

---

## مفاهیم کلیدی

### ساختار معنایی

- عنصر `<main>` باید به‌عنوان ظرف اصلی تمام محتوای قابل مشاهدهٔ صفحه استفاده شود.  
- هر عنصر `<section>` نشان‌دهندهٔ یک دسته یا موضوع ویدیویی مجزا در صفحه است.  
- استفاده از `<h1>` برای عنوان اصلی و `<h2>` برای عناوین بخش‌ها، سلسله‌مراتب مشخصی ایجاد می‌کند و دسترس‌پذیری را بهبود می‌دهد.

---

### جاسازی ویدیوها

- عنصر `<iframe>` امکان جاسازی ویدیو از پلتفرم‌های خارجی مانند یوتیوب یا ویمئو را فراهم می‌کند.  

ویژگی‌های ضروری شامل موارد زیر است:

- `src` : تعیین‌کنندهٔ نشانی ویدیوی جاسازی‌شده  
- `title` : توضیح محتوای ویدیو برای ابزارهای دسترس‌پذیری  
- `width` و `height` : مشخص‌کنندهٔ ابعاد نمایش فریم ویدیو  

---

### دسترس‌پذیری و بهترین روش‌ها

- هر ویدیوی جاسازی‌شده باید دارای ویژگی `title` توصیفی برای صفحه‌خوان‌ها باشد.  
- گروه‌بندی محتوای مرتبط (عنوان، متن و ویدیو) داخل عناصر `<section>`، چیدمان را منطقی و کاربرپسند می‌کند.

---

## الزامات چیدمان صفحه

- عنصر `main` تنها فرزند مستقیم `body` باشد.  
- یک `h1` موضوع کلی صفحه را معرفی کند.  
- یک پاراگراف بلافاصله بعد از `h1` برای ارائهٔ توضیح یا مقدمهٔ کوتاه قرار گیرد.  
- سپس سه بخش (`section`) وجود داشته باشد که هرکدام شامل موارد زیر هستند:  
  - یک `h2` برای عنوان ویدیو  
  - یک پاراگراف با توضیح کوتاه  
  - یک `iframe` برای جاسازی ویدیو با اندازهٔ مشخص و عنوان توصیفی  

---

## هدف نهایی

با انجام این چالش، یاد می‌گیرید چگونه محتوای چندرسانه‌ای را به‌صورت معنایی با استفاده از اچ‌تی‌ام‌ال سازمان‌دهی کنید و با ساختار مناسب و ویژگی‌های توصیفی، دسترس‌پذیری را بهبود دهید. این مهارت، پایه‌ای برای ساخت صفحات وب غنی، آموزنده و جذاب است.

</div>

