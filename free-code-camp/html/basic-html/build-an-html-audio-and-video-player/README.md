### Multimedia Page

This project demonstrates how to use HTML multimedia elements to create a page that plays both a video and an audio track. The goal is to understand how to structure a simple webpage that integrates interactive media content using semantic HTML elements.

#### Objective

Build a multimedia app that meets all the following user stories and passes the test requirements.

#### User Stories

1. The page should include an **h1** element that serves as the main title.
2. There should be **two section** elements to separate the video and audio content.
3. Inside the first section, include an **h2** element that provides a title for the video.
4. Below the **h2**, add a **video** element with the **controls** and **width** attributes (set width to 640).
5. The **video** element should contain a **source** element with a valid **src** and **type** attribute.

   * You can use:
     `https://cdn.freecodecamp.org/curriculum/labs/What is the map method and how does it work.mp4`
6. Inside the second section, include an **h2** element for the audio title.
7. Below that, add an **audio** element with the **controls** and **loop** attributes, and a **src** pointing to a valid audio file.

   * You can use:
     `https://cdn.freecodecamp.org/curriculum/js-music-player/sailing-away.mp3`
     or
     `https://cdn.freecodecamp.org/curriculum/js-music-player/we-are-going-to-make-it.mp3`

#### Goal

Create a web page that is functionally similar to the example project but reflects your own personal style. The project should use proper semantic HTML structure and provide an engaging way to showcase video and audio media on a single page.

---

<div dir="rtl">

# صفحهٔ چندرسانه‌ای

این پروژه نشان می‌دهد چگونه می‌توان با استفاده از عناصر چندرسانه‌ای اچ‌تی‌ام‌ال، صفحه‌ای ایجاد کرد که هم ویدیو و هم فایل صوتی را پخش کند.  
هدف این تمرین، درک ساختار یک صفحهٔ وب ساده است که محتوای رسانه‌ای تعاملی را با استفاده از عناصر معنایی اچ‌تی‌ام‌ال در خود جای می‌دهد.

---

## هدف

ایجاد یک برنامهٔ چندرسانه‌ای که تمام داستان‌های کاربری مشخص‌شده را پوشش دهد و تمام آزمون‌های مربوطه را با موفقیت پشت سر بگذارد.

---

## داستان‌های کاربری

۱. صفحه باید شامل یک عنصر عنوان اصلی باشد که به‌عنوان عنوان اصلی صفحه عمل کند.  

۲. باید دو بخش مجزا وجود داشته باشد تا محتوای ویدیو و صوت از یکدیگر جدا شوند.  

۳. در بخش اول، یک عنوان سطح دوم قرار گیرد که عنوان ویدیو را نمایش دهد.  

۴. در زیر این عنوان، یک عنصر ویدیو قرار داده شود که دارای کنترل‌های پخش بوده و عرض آن روی مقدار ۶۴۰ تنظیم شده باشد.  

۵. عنصر ویدیو باید شامل یک منبع باشد که نشانی معتبر و نوع فایل را مشخص کند.  

می‌توانید از این نشانی استفاده کنید:  
https://cdn.freecodecamp.org/curriculum/labs/What%20is%20the%20map%20method%20and%20how%20does%20it%20work.mp4  

۶. در بخش دوم، یک عنوان سطح دوم برای معرفی فایل صوتی قرار گیرد.  

۷. در زیر آن، یک عنصر صوتی قرار داده شود که دارای کنترل‌های پخش و پخش تکرارشونده باشد و نشانی آن به یک فایل صوتی معتبر اشاره کند.  

می‌توانید از یکی از این نشانی‌ها استفاده کنید:  
https://cdn.freecodecamp.org/curriculum/js-music-player/sailing-away.mp3  
یا  
https://cdn.freecodecamp.org/curriculum/js-music-player/we-are-going-to-make-it.mp3  

---

## هدف نهایی

ایجاد یک صفحهٔ وب که از نظر عملکردی مشابه پروژهٔ نمونه باشد، اما سبک و ساختار شخصی شما را منعکس کند.  
این پروژه باید از ساختار معنایی صحیح اچ‌تی‌ام‌ال استفاده کند و روشی جذاب برای نمایش هم‌زمان ویدیو و صوت در یک صفحه فراهم آورد.

</div>
