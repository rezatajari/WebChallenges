# Teacher Chatbot

This project is a **JavaScript console chatbot** designed to teach string manipulation concepts such as template literals, string length, character access, and substring positions. It demonstrates how to dynamically build sentences and use string properties in JavaScript.

---

## Demo

When you run the program, the console outputs a series of explanations and examples, including:

- A greeting from the bot
- Introduction to string manipulation concepts
- Examples of using string length
- Examples of accessing first, second, and last characters of words
- Examples of finding positions of substrings within sentences
- A farewell message

---

## Concepts Covered

- **Template literals** for dynamic sentence creation  
- **String length** to determine the number of characters in a word or phrase  
- **Accessing characters** using bracket notation to get specific letters  
- **Finding substring positions** to locate words or phrases within a sentence  

---

## How It Works

1. The bot introduces itself to the user.  
2. Example words are presented to demonstrate string properties.  
3. The program dynamically constructs sentences using template literals.  
4. It shows how to determine the length of strings.  
5. It demonstrates accessing the first, second, and last characters of a string.  
6. It locates substrings within a sentence using position-finding methods.  
7. The bot concludes with a friendly farewell.

---

## How to Run

1. Copy the project into a file named `stringBot.js`.  
2. Open your terminal or command prompt and navigate to the file’s directory.  
3. Run the script using Node.js.  
4. Observe the interactive console output from the bot.

---

## Technologies Used

- JavaScript (ES6)  
- Console output  
- String properties and methods  

---

# چت‌بات معلم

## معرفی پروژه

این پروژه یک **چت‌بات کنسولی با JavaScript** است که برای آموزش مفاهیم مربوط به **کار با رشته‌ها (String Manipulation)** طراحی شده است.  
در این برنامه، مفاهیمی مانند قالب‌های رشته‌ای (Template Literals)، طول رشته، دسترسی به کاراکترها و یافتن موقعیت زیررشته‌ها به‌صورت آموزشی و مرحله‌به‌مرحله نمایش داده می‌شود.

هدف این پروژه، نشان دادن نحوه ساخت جملات پویا و استفاده از ویژگی‌ها و متدهای رشته‌ای در JavaScript است.

---

## دمو (خروجی برنامه)

با اجرای برنامه، خروجی‌هایی مشابه موارد زیر در کنسول نمایش داده می‌شود:

- پیام خوش‌آمدگویی از طرف بات
- معرفی مفاهیم مربوط به کار با رشته‌ها
- مثال‌هایی از محاسبه طول رشته‌ها
- مثال‌هایی برای دسترسی به:
  - حرف اول
  - حرف دوم
  - حرف آخر کلمات
- مثال‌هایی از پیدا کردن موقعیت یک کلمه یا عبارت در داخل یک جمله
- پیام خداحافظی دوستانه

---

## مفاهیم پوشش داده‌شده

- استفاده از **Template Literals** برای ساخت جملات پویا
- استفاده از **طول رشته (string length)** برای تعیین تعداد کاراکترها
- دسترسی به کاراکترها با استفاده از **Bracket Notation**
- یافتن موقعیت زیررشته‌ها برای تشخیص محل یک کلمه یا عبارت در جمله

---

## نحوه کار برنامه

1. بات خودش را به کاربر معرفی می‌کند.
2. چند کلمه و جمله نمونه برای آموزش مفاهیم انتخاب می‌شوند.
3. جملات به‌صورت پویا با استفاده از Template Literals ساخته می‌شوند.
4. طول رشته‌ها محاسبه و نمایش داده می‌شود.
5. دسترسی به کاراکترهای اول، دوم و آخر رشته‌ها نمایش داده می‌شود.
6. موقعیت زیررشته‌ها در جمله مشخص می‌شود.
7. در پایان، بات با یک پیام دوستانه خداحافظی می‌کند.

---

## نحوه اجرا

1. کد پروژه را در فایلی با نام `stringBot.js` ذخیره کنید.
2. ترمینال یا Command Prompt را باز کرده و به مسیر فایل بروید.
3. برنامه را با Node.js اجرا کنید:
   ```bash
   node stringBot.js

4. خروجی آموزشی و مرحله‌به‌مرحله بات را در کنسول مشاهده کنید.
