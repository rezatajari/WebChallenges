# Build Technical Documentation Page 

## Project Overview
This project is part of FreeCodeCamp's **Responsive Web Design Certification** challenges. The objective of this challenge is to build a **technical documentation page** that is fully functional, responsive, and user-friendly. The page provides structured content for a technical topic, with a fixed navigation menu and smooth scrolling.

## User Stories
The project fulfills the following user stories:

1. **Main Documentation Area**  
   - The page contains a `<main>` element with `id="main-doc"` that holds the main content.

2. **Sections**  
   - At least five `<section>` elements with the class `main-section` are present.  
   - Each section has a `<header>` describing the topic.  
   - Each section has an `id` that matches its header text with underscores replacing spaces.

3. **Content Requirements**  
   - The sections include at least ten `<p>` elements, five `<code>` elements, and five `<li>` items total.  
   - Lists are properly nested inside `<ul>` or `<ol>`.

4. **Navbar**  
   - A `<nav>` element with `id="navbar"` contains a header and links.  
   - Links have the class `nav-link` and navigate to their corresponding sections.  
   - Navbar is fixed on the left side for desktops and remains visible during scrolling.

5. **Responsive Design**  
   - Media queries are used to make the page responsive on smaller screens.  
   - On mobile devices, the navbar moves to the top and the content fills the screen width.

6. **Styling**  
   - The project uses CSS for visual styling, including:  
     - Dark background for the navbar  
     - Smooth scrolling behavior  
     - Styled `<code>` elements  
     - Proper spacing and typography for readability

## How to Use
1. Open `index.html` in a browser.  
2. Click on the links in the navbar to scroll smoothly to the corresponding section.  
3. Resize the browser window to see the responsive behavior of the page.

## Technologies Used
- HTML5  
- CSS3 (including Flexbox, positioning, and media queries)

## Project Status
 Completed and fully functional.  
 Responsive and visually structured.  

---

# ساخت صفحه مستندات فنی

## معرفی پروژه
این پروژه بخشی از چالش‌های گواهی **طراحی وب واکنش‌گرا** در freeCodeCamp است.  
هدف این تمرین، ساخت یک صفحه مستندات فنی کامل، واکنش‌گرا و کاربرپسند است. این صفحه محتوای فنی را به‌صورت ساخت‌یافته نمایش می‌دهد و دارای منوی ناوبری ثابت و اسکرول نرم می‌باشد.

---

## داستان‌های کاربر
این پروژه الزامات زیر را پوشش می‌دهد:

### بخش اصلی مستندات
- صفحه دارای یک عنصر `<main>` با شناسه `main-doc` است که محتوای اصلی را در خود جای می‌دهد.

### بخش‌ها
- حداقل پنج عنصر `<section>` با کلاس `main-section` وجود دارد.
- هر بخش دارای یک `<header>` برای توضیح موضوع است.
- هر بخش یک `id` دارد که متن هدر آن را نشان می‌دهد (با جایگزینی فاصله‌ها با زیرخط).

### الزامات محتوا
- در مجموع حداقل:
  - ده پاراگراف `<p>`
  - پنج عنصر `<code>`
  - پنج آیتم لیست `<li>`
- لیست‌ها به‌درستی داخل `<ul>` یا `<ol>` قرار گرفته‌اند.

### نوار ناوبری
- یک عنصر `<nav>` با شناسه `navbar` وجود دارد که شامل:
  - یک هدر
  - لینک‌های ناوبری
- لینک‌ها دارای کلاس `nav-link` هستند و به بخش مربوطه هدایت می‌شوند.
- در نمایش دسکتاپ، نوار ناوبری در سمت چپ ثابت است و هنگام اسکرول همیشه قابل مشاهده می‌ماند.

### طراحی واکنش‌گرا
- از مدیا کوئری‌ها برای سازگاری با صفحه‌نمایش‌های کوچک استفاده شده است.
- در موبایل، نوار ناوبری به بالای صفحه منتقل می‌شود و محتوا تمام عرض صفحه را پر می‌کند.

### استایل‌دهی
- استفاده از CSS برای زیباسازی بصری، شامل:
  - پس‌زمینه تیره برای نوار ناوبری
  - اسکرول نرم
  - استایل‌دهی مناسب برای عناصر `<code>`
  - فاصله‌گذاری و تایپوگرافی مناسب برای خوانایی بهتر

---

## نحوه استفاده
1. فایل `index.html` را در مرورگر باز کنید.
2. روی لینک‌های نوار ناوبری کلیک کنید تا به‌صورت نرم به بخش مربوطه اسکرول شود.
3. اندازه پنجره مرورگر را تغییر دهید تا رفتار واکنش‌گرای صفحه را مشاهده کنید.

---

## فناوری‌های استفاده‌شده
- HTML5  
- CSS3  
  - فلکس‌باکس  
  - موقعیت‌دهی عناصر  
  - مدیا کوئری‌ها  

---

## وضعیت پروژه
✅ تکمیل شده و کاملاً قابل استفاده  
✅ واکنش‌گرا و دارای ساختار بصری منظم  

